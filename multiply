#include <stdio.h>

int main()
{
    int mul(int, int);
    int x,n;
    printf("enter values");
    scanf("%d%d",&x,&n);
    printf("multiply = %d",mul(x,n));
    return 0;
}
int mul (int x, int n)
 {
     return(x*n);
 }
